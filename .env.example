# Environment variable template for AI Marketing OS
#
# Copy this file to `.env` and fill in real values. Never commit `.env` or any
# real secrets to the repository.
#
# Concrete variables will be added here as components are specified by the
# project architect. Keep this file in sync with the configuration the system
# actually reads.

# Application environment: dev | staging | prod
APP_ENV=dev
